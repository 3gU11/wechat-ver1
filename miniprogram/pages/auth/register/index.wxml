<view class="page">
  <view class="brand-strip">
    <image class="brand-logo" src="/images/rj-logo.png" mode="aspectFit" />
    <view>
      <view class="brand-name">经销商门户</view>
      <view class="brand-subtitle">账号申请</view>
    </view>
  </view>

  <view class="section-title">填写注册信息</view>
  <view class="hint">当前微信账号会绑定到本次申请。审核通过后，再次微信登录即可进入小程序。</view>

  <view class="card form-card">
    <view class="field">
      <view class="label">账号类型</view>
      <picker mode="selector" range="{{roles}}" range-key="label" value="{{roleIndex}}" bindchange="onRoleChange">
        <view class="picker-value">{{roles[roleIndex].label}}</view>
      </picker>
    </view>
    <view class="field">
      <view class="label">公司 / 名称</view>
      <input placeholder="请输入公司或大区经理名称" data-field="companyName" bindinput="onInput" />
    </view>
    <view class="field">
      <view class="label">姓名</view>
      <input placeholder="请输入申请人姓名" data-field="contactName" bindinput="onInput" />
    </view>
    <view class="field">
      <view class="label">手机号</view>
      <input placeholder="请输入手机号" type="number" data-field="phone" bindinput="onInput" />
    </view>
    <view class="field">
      <view class="label">{{form.role === 'regional_manager' ? '管理大区' : '所在地区'}}</view>
      <input placeholder="例如 江苏苏州 / 浙江宁波 / 华东" data-field="region" bindinput="onInput" />
    </view>
    <view wx:if="{{form.role === 'dealer'}}" class="field">
      <view class="label">所属大区经理</view>
      <picker mode="selector" range="{{regionalManagerNames}}" value="{{regionalManagerIndex}}" bindchange="onRegionalManagerChange">
        <view class="picker-value">{{form.regionalManagerName || '请选择所属大区经理'}}</view>
      </picker>
    </view>
    <view class="field">
      <view class="label">选择瑞钧的理由 / 合作说明</view>
      <textarea placeholder="请填写资质、合作背景、选择瑞钧的理由或其他说明" data-field="remark" bindinput="onInput"></textarea>
    </view>
  </view>

  <view class="agreement-block">
    <checkbox-group bindchange="onAgreementChange">
      <label class="agreement-row">
        <checkbox value="agree" checked="{{agreedToTerms}}" color="#1F4F9A" />
        <text class="agreement-text">我已阅读并同意</text>
      </label>
    </checkbox-group>
    <view class="agreement-links">
      <text class="agreement-link" data-type="terms" bindtap="openAgreement">《用户服务协议》</text>
      <text class="agreement-text">和</text>
      <text class="agreement-link" data-type="privacy" bindtap="openAgreement">《隐私政策》</text>
    </view>
    <view class="agreement-note">手机号用于经销商身份审核、登录验证、订单联系及业务通知。</view>
  </view>

  <button class="primary-btn submit-btn" loading="{{submitting}}" bindtap="submit">提交审核</button>
</view>
